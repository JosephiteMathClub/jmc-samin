import EventRegister from '@/views/EventRegister';

export const metadata = {
  title: 'Event Registration - Josephite Math Club',
  description: 'Register for solo and team events and competitions at Josephite Math Club.',
};

export default function EventRegisterPage() {
  return <EventRegister />;
}
